Ablauf einer Simulation

1. Definition des Start- und Enddatums

2. getTime(start, end)
-> stellt Zeitarray bereit (15 Minuten)

3. getNormSLP(start, end)
-> berechnet G0, L0 und PHH Lastprofile

4. getWeatherBC(start, end, region)
-> gibt Einstrahlungs- und Temperaturwerte zur�ck

5. define all statisitic distributions
-> agents distro
-> building distro
-> PHH portions
-> COC distros
-> district heating portions
-> PV portion
-> building type distribution

5. initialize CellManager
-> create AgentManagers for free standing houses (PHHfree, BSLaFree, BSLcFree)
	-> getCOC()
		-> berechnet COC-Werte f�r nAgents
		-> schreibt COCfactor array
	-> berechnet LoadProfile_e = SLP * COC * (0,8 + rand(timesteps, nAgents) * 0,4)
	-> schreibt LoadProfile_e
	-> GetHotWaterDemand()
		-> berechnet LoadProfile_t = (684.7 * self.COCfactor + 314.4) * 1e3 / 8760 .* (0.8 + rand(length(time), self.nAgents) * 0.4) .* HotWaterProfile(time.Hour+1)   % COCfactor[1,nAgents]*random[time,nAgents]*[24,1]
-> create SUBmanager
	- intialize abstract class
		-> choose PV buildings (PVmask)
		-> calculate PV area APV = (0,8 + rand(1, self.nPV) * 0.4) * 1e3 / Eg
	- get thermal load: for classIdx = 1:length(fieldnames(refData.Uvalues)) ???

	- map agents to buildings: get masks with length nBuildings (only FSH)
	- calculate APV array ??? versteh die Syntax einfach nicht

standard load profiles:
PHH ... private Haushalte
BSL ... Betriebe mit Standardlastprofil

standard building types with classes, modernization, ventilation:
FSH ... free standing house
REH ... row end house
SAH ... small apartment houses
BAH ... big apartment houses

building types:
SUB ... single user building
MUB ... multi user building




ToutN = -13,8�C ???
% H�user bleiben leer?